spring:
  datasource:
    url: jdbc:h2:mem:adt2025
    username: adt2025
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # validate / update / create / create-drop
      properties:
        hibernate:
          format-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console # pra acessar o banco via browser
      settings:
        trace: false
        web-allow-others: false